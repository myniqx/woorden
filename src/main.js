import './style.css';
import { initUI } from './modules/ui.js';

// Initialize the app when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
  console.log('🇳🇱 Woorden App initialized');
  initUI();
});
